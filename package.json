{
  "private": true,
  "name": "root",
  "version": "0.0.1",
  "workspaces": {
    "packages": [
      "packages/ui",
      "packages/app"
    ]
  },
  "scripts": {
    "test:ui": "yarn --cwd packages/ui run test",
    "develop": "netlify dev",
    "develop:ui": "yarn --cwd packages/ui run develop",
    "build:storybook": "yarn --cwd packages/ui run build-storybook",
    "build:app": "yarn --cwd packages/app run build"
  },
  "author": "@mattersupply.co",
  "license": "UNLICENSED",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^12",
    "eslint-plugin-jsdoc": "^24.0.0",
    "import": "^0.0.6",
    "typescript": "^3"
  }
}
